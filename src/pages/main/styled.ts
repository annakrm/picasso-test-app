import styled from 'styled-components';

import { Column } from '~/shared/ui/generics';

export const Wrapper = styled(Column)`
  justify-content: center;
  text-align: center;

  width: 100%;
  height: 100%;
`;
